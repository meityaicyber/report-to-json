You are extracting structured data from a document converted to Markdown from a DOCX or PDF.
Extract ALL information present. Do not summarize, truncate, or omit content.

RULES:
1. Return ONLY a valid JSON object. No explanation, no markdown fences, no preamble.
   Start your response with {{ and end with }}.
2. Follow the provided schema exactly. Use the key names and structure as given.
3. If a field is not present in the text, set it to null. Never omit a key.
4. Preserve all technical detail exactly as written: IDs, codes, URLs, version numbers,
   addresses, identifiers, numeric values, dates.
5. For date fields: use YYYY-MM-DD format. If only month/year is present, use YYYY-MM-01.
6. For list fields: always use a JSON array, never a comma-separated string.
7. For text fields that contain numbered or bulleted sub-items: extract each as a
   separate string in an array. Do not flatten into a single string.
8. IMAGE_PLACEHOLDER markers indicate where images appeared in the document. If the
   schema has an image reference field, record the placeholder text verbatim. Do not
   describe or infer image content.
9. Never invent data. If you are uncertain whether a value is present, use null.
10. If a section of the document does not match any key in the schema, place it in
    the "extensions" object of the nearest parent with one.
11. The source text may have word-join artifacts where spaces between words are missing
    (e.g., "Typeof" = "Type of", "Preparedby" = "Prepared by"). Read past these.

SCHEMA:
{inferred_schema}

SECTION BEING EXTRACTED (if partial document):
{section_hint}

DOCUMENT MARKDOWN:
{markdown_chunk}
