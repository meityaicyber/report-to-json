You are analyzing a document that has been converted to Markdown from a DOCX or PDF file.
Your task is to propose a JSON schema that captures ALL information in this document.

RULES:
1. Return ONLY a valid JSON object representing your proposed schema. No explanation,
   no markdown fences, no preamble. Start your response with {{ and end with }}.
2. The schema must have a "sections" key at the top level. Each section you identify
   in the document becomes a key under "sections", named in snake_case.
3. For each section, describe the expected structure: is it a flat object, a list of
   items, a nested object? What fields does each item or the object have?
4. Use JSON Schema draft-07 format: type, properties, items, required, etc.
5. Use "null" as the type for any field that may be absent in some documents.
6. If a section contains a repeating list of structured items (e.g. findings, entries,
   records, assets), model it as an array with an items schema.
7. Do not invent fields that are not present in the document. Only describe what you see.
8. IMAGE_PLACEHOLDER markers in the document indicate where images appeared. Do not
   create schema fields for image content - note their positions only if relevant.
9. Add an "extensions" object to any section or item schema where you suspect
   additional fields might appear in other documents of the same type.

EXAMPLE OUTPUT (for a hypothetical invoice document):
{{"sections": {{"header": {{"type": "object", "properties": {{"invoice_number": {{"type": "string"}}, "date": {{"type": "string"}}, "vendor": {{"type": "string"}}}}}}, "line_items": {{"type": "array", "items": {{"type": "object", "properties": {{"description": {{"type": "string"}}, "quantity": {{"type": "integer"}}, "price": {{"type": "number"}}}}}}}}, "total": {{"type": "object", "properties": {{"subtotal": {{"type": "number"}}, "tax": {{"type": "number"}}, "total": {{"type": "number"}}}}}}}}}}

DOCUMENT MARKDOWN:
{markdown_chunk}
